# CLAUDE.md

The instructions for working on this repository live in
[`AGENTS.md`](AGENTS.md). Read that file.

It is kept as the single source so that every harness — Claude Code, Factory,
OpenCode, Codex, Cursor — reads the same rules. This file exists only because
Claude Code looks for it by name.
