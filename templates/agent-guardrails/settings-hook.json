{
  "_comment": "Merge this into <host>/.claude/settings.json. `agent-ops bootstrap --guardrails` does the merge for you when jq is available, and prints this file to paste when it is not.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "$CLAUDE_PROJECT_DIR/.claude/hooks/block-dangerous-git.sh"
          }
        ]
      }
    ]
  }
}
